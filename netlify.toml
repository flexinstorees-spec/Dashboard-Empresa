[build]
  command = "pnpm --filter @workspace/painel-financeiro run build"
  publish = "artifacts/painel-financeiro/dist/public"

[build.environment]
  NODE_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
